# DayTrade – install everything for server (clone + pip install -r requirements.txt)
# Python 3.10+ recommended (3.13 compatible)

# Build
setuptools>=60,<82

# API & server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Database (PostgreSQL)
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.2
alembic==1.12.1

# ML & training (next-minute LSTM, price predictor, curves)
torch>=2.6.0
optuna>=3.0.0
scipy>=1.10.0
torchvision>=0.21.0
numpy>=1.26.0
matplotlib>=3.7.0
Pillow>=10.4.0
opencv-python==4.8.1.78

# Data & time
pytz==2023.3
python-dateutil==2.8.2

# Screenshot / scraping (optional for data collection)
selenium==4.15.2
webdriver-manager==4.0.1

# Databento (OHLCV ingestion; includes zstandard)
databento>=0.71.0

# Scheduler & env
python-dotenv==1.0.0
requests==2.31.0
APScheduler>=3.10.0

# Dev & tests (optional on server; needed for pytest)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx>=0.25.0,<0.28.0
black==23.11.0
flake8==6.1.0
